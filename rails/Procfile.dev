web: bin/rails server
# [always] keeps the watcher alive when stdin is closed — under foreman in
# the devcontainer the watch process gets no TTY and would exit immediately.
css: bin/rails tailwindcss:watch[always]
